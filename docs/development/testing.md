# 测试策略

## 当前基线

2026-09-06 执行 `mvn test`：4 个 Reactor 项目成功，执行 2 个测试，0 失败、0 错误、0 跳过。实际测试集中在分页查询处理器的幂等行为。JaCoCo 未生成执行数据。

## 变更应覆盖

- 处理器顺序、短路、异常和清理逻辑。
- SQL 解析缓存的命中、复制、失败恢复和并发行为。
- 分页 count、排序合并、参数映射、空结果及禁用 count。
- MySQL 与 PostgreSQL 方言输出。
- Starter 的条件装配、用户 Bean 覆盖和配置绑定。
- 真实 MyBatis + 数据库的集成路径。

测试成功不等于覆盖充分。提交时应说明新增测试、未覆盖路径和是否产生覆盖率报告。
