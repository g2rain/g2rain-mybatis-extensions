# 完成标准

一项变更完成时应满足：

- 需求与影响模块清晰，未夹带无关修改。
- 公共行为、缺陷修复和主要边界有自动化测试。
- `mvn test` 通过；发布候选执行 `mvn clean verify`。
- API、配置、架构、兼容性或发布流程变化已更新 `docs/`。
- 未提交生成物、IDE 文件、密钥或本地配置。
- 明确记录未验证数据库、兼容性、覆盖率和剩余风险。
- 发布前版本、标签、变更日志、README 示例保持一致。
